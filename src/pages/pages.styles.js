import styled from "@emotion/styled";

export const GlobalContainer = styled.div((props) => ({
  margin: 0,
  padding: 0,
  position: "fixed",
  width: "100%",
  height: "100%",
  backgroundColor: "#1d1d1d",
}));
